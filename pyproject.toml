[project]
name = "document-automater"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "click>=8.1",
    "playwright>=1.40",
    "requests>=2.31",
    "PyYAML>=6.0",
]

[project.scripts]
fetch-reports = "sec_reports.main:run"

[dependency-groups]
dev = []

